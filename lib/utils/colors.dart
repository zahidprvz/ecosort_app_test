import 'package:flutter/material.dart';

// Updated color scheme
const mobileBackgroundColor =
    Color.fromRGBO(0, 99, 137, 1); // A mix of blue and green
const webBackgroundColor =
    Color.fromRGBO(0, 123, 167, 1); // Another mix of blue and green for web
const mobileSearchColor = Color.fromRGBO(
    0, 149, 135, 1); // A different mix of blue and green for contrast
const blueColor = Color.fromRGBO(0, 149, 246, 1); // Keeping the blue color
const primaryColor = Color.fromRGBO(
    240, 248, 255, 1); // Light blue (Alice Blue) for a calm, trustworthy feel
const secondaryColor = Color.fromRGBO(
    144, 238, 144, 1); // Light green (Light Green) for health association
